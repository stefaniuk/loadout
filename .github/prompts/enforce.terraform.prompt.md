---
agent: agent
argument-hint: "Optional: target paths/files to audit (defaults to whole repository)"
description: Enforce repository-wide compliance with terraform.instructions.md
---

This prompt delegates to the **enforcement-audit** skill.

Run the `enforcement-audit` skill for **Terraform**.

```text
$ARGUMENTS
```

Pass through the user input above as additional context for the audit.
