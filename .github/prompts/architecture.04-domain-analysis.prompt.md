---
agent: agent
description: Domain analysis (DDD) to document bounded contexts, language, events, and context map (evidence-first)
---

This prompt delegates to the **architecture-docs** skill.

Run the `architecture-docs` skill for step **04** (domain-analysis).

```text
$ARGUMENTS
```

Pass through the user input above as additional context.
