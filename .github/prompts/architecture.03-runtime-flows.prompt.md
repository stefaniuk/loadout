---
agent: agent
description: Document key runtime flows with diagrams (trigger → orchestration → data lineage), evidence-first
---

This prompt delegates to the **architecture-docs** skill.

Run the `architecture-docs` skill for step **03** (runtime-flows).

```text
$ARGUMENTS
```

Pass through the user input above as additional context.
