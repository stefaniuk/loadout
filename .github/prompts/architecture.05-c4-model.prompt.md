---
agent: agent
description: Produce C4 model diagrams (Context, Container, Component) in LikeC4 format (https://likec4.dev/, evidence-first, consistent naming)
---

This prompt delegates to the **architecture-docs** skill.

Run the `architecture-docs` skill for step **05** (c4-model).

```text
$ARGUMENTS
```

Pass through the user input above as additional context.
