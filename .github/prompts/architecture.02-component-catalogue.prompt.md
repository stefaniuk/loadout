---
agent: agent
argument-hint: "Optional: target paths or scope for the component-catalogue step"
description: Create component-level summaries (responsibilities, interfaces, data, and extension points)
---

This prompt delegates to the **architecture-docs** skill.

Run the `architecture-docs` skill for step **02** (component-catalogue).

```text
$ARGUMENTS
```

Pass through the user input above as additional context.
